#include<bits/stdc++.h>
using namespace std;
int main ()
{
  float a , b ;
   cin >> a >> b;
 cout << showpoint;
  // i want to show point.then i am using  showpoint .
   float sum = a + b ;
   cout << sum << endl;
   // i am not intreast show point. Then i using noshowpoint . 
 cout << noshowpoint;
   float mul = a * b ;
   cout << mul << endl;

   float div = a / b ;
   cout << div << endl;
    return 0;
}