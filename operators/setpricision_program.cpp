#include<bits/stdc++.h>

using namespace std;
int main ()
{
  float a , b ;
   cin >> a >> b;
 cout << showpoint;
  cout <<setprecision(10);
   float sum = a + b ;
   cout << sum << endl;
   
 cout << noshowpoint;
   float mul = a * b ;
   cout << mul << endl;

 cout << showpoint;
 cout << fixed;
  cout <<setprecision(5);

   float div = a / b ;
   cout << div << endl;
    return 0;
}